#include<iostream.h>
#include<conio.h>
#include<math.h>
class Rectangle{
 private:
  float l;
  float w;
 public:
   void input(){
     cout<<"Enter l:";cin>>l;
     cout<<"Enter w:";cin>>w;
     }
   void output(){
     cout<<"Output l:"<<l<<endl;
     cout<<"Output w:"<<w<<endl;
     }
     float Area(){
     return l*w;
     }
   };
   void main(){
    Rectangle p1,p2,p3,max;
     cout<<"Enter area of Rectangle p1:" <<endl; p1.input();
     cout<<"Enter area of Rectangle p2:" <<endl; p2.input();
     cout<<"Enter area of Rectangle p3:" <<endl; p3.input();

     max=p1;
      if(max.area()<p2.area())max=p2;
      if(max.area()<p3.area())max=p3;
     cout<<"Max area of rectangle is:"<<endl;
      max.output();
    getch();
    }


