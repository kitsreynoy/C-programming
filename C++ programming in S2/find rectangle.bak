#include<iostream.h>
#include<conio.h>
#include<math.h>
class Rectangle{
 private:
  float l;
  float w;
 public:
   void input(){
     cout<<"Enter l:";cin>>l;
     cout<<"Enter w:";cin>>w;
     }
   void output(){
     cout<<"Output l:"<<l<<endl;
     cout<<"Output w:"<<w<<endl;
     }
     float Area(){
     return l*w;
     }
   };
   void main(){
    Rectangle p1,p2,p3,max;
     cout<<"Enter area of rectangle p1\n"; p1.input();
     cout<<"Enter area of rectangle p2\n"; p2.input();
     cout<<"Enter area of rectangle p3\n"; p3.input();
     cout<<"Output p1\n"; p1.output();
     cout<<"Output p2\n"; p2.output();
     cout<<"Output p3\n"; p3.output();
     max=p1;
      if(max.area()<p2.area())max=p2;
      if(max.area()<p3.area())max=p3;
     cout<<"Max area of rectangle is:"<<endl;
      max.output();
    getch();
    }


